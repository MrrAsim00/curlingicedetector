basic.forever(function () {
	
})
basic.forever(function () {
    // Displays the current temperature on the LED grid
    basic.showNumber(input.temperature())
    basic.showString("C*")
    basic.pause(2000)
})
